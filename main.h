#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>

int _putchar(char c);
int _printf(const char *format, ...);
int print_num(int num);
void print_string(char *s);
int strlenn(const char *s);

#endif /* MAIN_H */
